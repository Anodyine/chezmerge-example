#!/usr/bin/env bash

set -euo pipefail

# Manual profile setup is intentionally not run automatically on apply.
# Run this explicitly when you want to change git/profile settings:
#   bash "{{ .chezmoi.sourceDir }}/scripts/setup-profile.sh"
:
