#!/usr/bin/env bash

# {{ if or (eq .chezmoi.hostname "ml-server") (eq .chezmoi.hostname "ml-server-2") }}

# Deprecated: OpenRGB setup now lives in run_onchange_after_55-sync-openrgb-i2c.sh.tmpl
# so repeated chezmoi applies use a single idempotent code path.

# {{ end }}
