# -----------------------------------------------------
# arch-surface workspace rules
# Laptop panel is always workspace 1 when present.
# External monitors are assigned at runtime by
# ~/.config/hypr/scripts/assign-workspaces.sh based on monitor description.
# -----------------------------------------------------

workspace = 1, monitor:eDP-1, persistent:true, default:true
